#include <iostream>
using namespace::std

int main()
{
	int q,i;
	cin>>q;
	int nums [q] [2];
	queue<int>students;
	for(i=0;i<q;i++)
	{
		cin>>action;
		if(action==E)
		{
			cin>>nums[i];
			nq.push(nums[i]);
			if(nums[i][
			
		}
		else if(action==D)
		{
			nq.pop();
		}
         }
