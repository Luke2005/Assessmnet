#include <iostream> 
using namespace::std;

int main()
{	
	int n;
	cin >> n;
	int *nums = new int[n];
	int unique = 0;
	int *counts= new int[n];
        for (i=0;i<n;i++)
    	{
		cin >> m;
               
        	for(j=0;j<unique;j++)
        	{
	            if(m == nums[j]) break;
                }
		if(j < unique) {
			counts[j] ++;
		}
		else { 
			nums[unique] = m;
			counts[unique] = 1;
			unique ++;
		}
             }
         for(i=0;i<n;i++
     
cout<<popular<<endl;
}
        
